from kore.common import *
# from modules.ci_and_ds_tools import POINT

def editFile1(TAB):
    TAB.num = 1
    if TAB.fileName1 == "im1.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName1)
    
def editFile2(TAB):
    TAB.num = 2
    if TAB.fileName2 == "im2.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName2)
    
def editFile3(TAB):
    TAB.num = 3
    if TAB.fileName3 == "im3.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName3)
    

def editFile4(TAB):
    TAB.num = 4
    if TAB.fileName4 == "im4.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName4)
    
def editFile5(TAB):
    TAB.num = 5
    if TAB.fileName5 == "im5.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName5)
    
def editFile6(TAB):
    TAB.num = 6
    if TAB.fileName6 == "im6.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName6)
    
def editFile7(TAB):
    TAB.num = 7
    if TAB.fileName7 == "im7.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName7)
    
def editFile8(TAB):
    TAB.num = 8
    if TAB.fileName8 == "im8.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName8)
    
def editFile9(TAB):
    TAB.num = 9
    if TAB.fileName9 == "im9.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName9)
    
def editFile10(TAB):
    TAB.num = 10
    if TAB.fileName10 == "im10.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=TAB.num)
        TAB.dialog.display(TAB.fileName10)

def editFile11(TAB):
    TAB.num = 11
    if TAB.fileName11 == "im11.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=11)
        TAB.dialog.display(TAB.fileName11)
    
def editFile12(TAB):
    TAB.num = 12
    if TAB.fileName12 == "im12.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=12)
        TAB.dialog.display(TAB.fileName12)
    
def editFile13(TAB):
    TAB.num = 13
    TAB.dialog = EDIT(TAB, num=13)
    if TAB.fileName13 == "im13.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=13)
        TAB.dialog.display(TAB.fileName13)
    
def editFile14(TAB):
    TAB.num = 14
    TAB.dialog = EDIT(TAB, num=14)
    if TAB.fileName14 == "im14.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=14)
        TAB.dialog.display(TAB.fileName14)

def editFile15(TAB):
    TAB.num = 15
    TAB.dialog = EDIT(TAB, num=15)
    if TAB.fileName15 == "im15.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=15)
        TAB.dialog.display(TAB.fileName15)
    
def editFile16(TAB):
    TAB.num = 16
    TAB.dialog = EDIT(TAB, num=16)
    if TAB.fileName16 == "im16.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=16)
        TAB.dialog.display(TAB.fileName16)

def editFile17(TAB):
    TAB.num = 17
    TAB.dialog = EDIT(TAB, num=17)
    if TAB.fileName17 == "im17.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=17)
        TAB.dialog.display(TAB.fileName17)
    
def editFile18(TAB):
    TAB.num = 18
    TAB.dialog = EDIT(TAB, num=18)
    if TAB.fileName18 == "im18.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=18)
        TAB.dialog.display(TAB.fileName18)
    
def editFile19(TAB):
    TAB.num = 19
    TAB.dialog = EDIT(TAB, num=19)
    if TAB.fileName19 == "im19.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=19)
        TAB.dialog.display(TAB.fileName19)
    

def editFile20(TAB):
    TAB.num = 20
    TAB.dialog = EDIT(TAB, num=20)
    if TAB.fileName20 == "im20.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=20)
        TAB.dialog.display(TAB.fileName20)
    
def editFile21(TAB):
    TAB.num = 21
    TAB.dialog = EDIT(TAB, num=21)
    if TAB.fileName21 == "im21.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=21)
        TAB.dialog.display(TAB.fileName21)
    
def editFile22(TAB):
    TAB.num = 22
    TAB.dialog = EDIT(TAB, num=22)
    if TAB.fileName22 == "im22.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=22)
        TAB.dialog.display(TAB.fileName22)

def editFile23(TAB):
    TAB.num = 23
    TAB.dialog = EDIT(TAB, num=23)
    if TAB.fileName23 == "im23.png":
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=23)
        TAB.dialog.display(TAB.fileName23)

def editFile24(TAB):
    TAB.num = 24
    TAB.dialog = EDIT(TAB, num=24)
    if TAB.fileName24 == "im24.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=24)
        TAB.dialog.display(TAB.fileName24)
    
def editFile25(TAB):
    TAB.num = 25
    TAB.dialog = EDIT(TAB, num=25)
    if TAB.fileName25 == "im25.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=25)
        TAB.dialog.display(TAB.fileName25)

def editFile26(TAB):
    TAB.num = 26
    TAB.dialog = EDIT(TAB, num=26)
    if TAB.fileName26 == "im26.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=26)
        TAB.dialog.display(TAB.fileName26)

def editFile27(TAB):
    TAB.num = 27
    TAB.dialog = EDIT(TAB, num=27)
    if TAB.fileName27 == "im27.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=27)
        TAB.dialog.display(TAB.fileName27)
    
def editFile28(TAB):
    TAB.num = 28
    TAB.dialog = EDIT(TAB, num=28)
    if TAB.fileName28 == "im28.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=28)
        TAB.dialog.display(TAB.fileName28)
    
def editFile29(TAB):
    TAB.num = 29
    TAB.dialog = EDIT(TAB, num=29)
    if TAB.fileName29 == "im29.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=29)
        TAB.dialog.display(TAB.fileName29)
    
def editFile30(TAB):
    TAB.num = 30
    TAB.dialog = EDIT(TAB, num=30)
    if TAB.fileName30 == "im30.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=30)
        TAB.dialog.display(TAB.fileName30)
    
def editFile31(TAB):
    TAB.num = 31
    TAB.dialog = EDIT(TAB, num=31)
    if TAB.fileName31 == "im31.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=31)
        TAB.dialog.display(TAB.fileName31)
    
def editFile32(TAB):
    TAB.num = 32
    TAB.dialog = EDIT(TAB, num=32)
    if TAB.fileName32 == "im32.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=32)
        TAB.dialog.display(TAB.fileName32)
    
def editFile33(TAB):
    TAB.num = 33
    TAB.dialog = EDIT(TAB, num=33)
    if TAB.fileName33 == "im33.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog = EDIT(TAB, num=33)
        TAB.dialog.display(TAB.fileName33)
    
def editFile34(TAB):
    TAB.num = 34
    TAB.dialog = EDIT(TAB, num=34)
    if TAB.fileName34 == "im34.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName34)
    

def editFile35(TAB):
    TAB.num = 35
    TAB.dialog = EDIT(TAB, num=35)
    if TAB.fileName35 == "im35.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName35)

def editFile36(TAB):
    TAB.num = 36
    TAB.dialog = EDIT(TAB, num=36)
    if TAB.fileName36 == "im36.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName36)

def editFile37(TAB):
    TAB.num = 37
    TAB.dialog = EDIT(TAB, num=37)
    if TAB.fileName37 == "im37.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName37)

def editFile38(TAB):
    TAB.num = 38
    TAB.dialog = EDIT(TAB, num=38)
    if TAB.fileName38 == "im38.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName38)
    
def editFile39(TAB):
    TAB.num = 39
    TAB.dialog = EDIT(TAB, num=39)
    if TAB.fileName39 == "im39.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName39)

def editFile40(TAB):
    TAB.num = 40
    TAB.dialog = EDIT(TAB, num=40)
    if TAB.fileName40 == "im40.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message_erreur1()
    else:
        TAB.dialog.display(TAB.fileName40)

def editFile41(TAB):
    TAB.num = 41
    TAB.dialog = EDIT(TAB, num=41)
    if TAB.fileName41 == "im41.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName41)

def editFile42(TAB):
    TAB.dialog = EDIT(TAB, num=42)
    if TAB.fileName42 == "im42.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName42)
    

def editFile43(TAB):
    TAB.dialog = EDIT(TAB, num=43)
    if TAB.fileName43 == "im43.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName43)
    
def editFile44(TAB):
    TAB.dialog = EDIT(TAB, num=44)
    if TAB.fileName44 == "im44.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName44)
    
def editFile45(TAB):
    TAB.dialog = EDIT(TAB, num=45)
    if TAB.fileName45 == "im45.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName45)

def editFile46(TAB):
    TAB.dialog = EDIT(TAB, num=46)
    if TAB.fileName46 == "im46.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName46)
    
def editFile47(TAB):
    TAB.dialog = EDIT(TAB, num=47)
    if TAB.fileName47 == "im47.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName47)
    
def editFile48(TAB):
    TAB.dialog = EDIT(TAB, num=48)
    if TAB.fileName48 == "im48.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName48)
    
def editFile49(TAB):
    TAB.dialog = EDIT(TAB, num=49)
    if TAB.fileName49 == "im49.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName49)
    
def editFile50(TAB):
    TAB.dialog = EDIT(TAB, num=50)
    if TAB.fileName50 == "im50.png":
        TAB.dialog = MESSAGE()
        TAB.dialog.message()
    else:
        TAB.dialog.display(TAB.fileName50)
    
